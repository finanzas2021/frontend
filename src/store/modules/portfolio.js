import portfolioService from "@/portfolio/services/portfolio.service";

const state = {
  all: [],
};

const mutations = {};

const actions = {};

const getters = {};

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  getters,
};
